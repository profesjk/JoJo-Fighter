{
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "lint:fix": "turbo lint:fix",
    "test": "turbo test",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "contracts:deploy:solo": "turbo run deploy:solo",
    "contracts:deploy:testnet": "turbo run deploy:testnet",
    "contracts:deploy:mainnet": "turbo run deploy:mainnet"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "prettier": "^3.2.5",
    "turbo": "latest"
  },
  "packageManager": "yarn@1.22.21",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "react-router-dom": "^6.24.0"
  }
}
